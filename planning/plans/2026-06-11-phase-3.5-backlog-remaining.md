# Phase 3.5 — remaining backlog (post Sprint 2)

Rewritten 2026-06-11 after **Sprint 1 shipped** (`6c5372d`); updated 2026-06-12 after **Sprint 2
shipped** (`a2289d5`). Supersedes the open items in `planning/plans/2026-06-03-phase-3.5-backlog.md`
(kept for history). Sprint-1 plan: `~/.claude/plans/dazzling-splashing-oasis.md`.
Sprint-2 plan: `~/.claude/plans/hi-claude-can-you-giggly-beacon.md`.

## ✅ Done in Sprint 1 (2026-06-11)
- **#6** — `quote.ts` → FMP-primary + bounded concurrency (Polygon/EODHD now fallback only; fixes `.L` DCF Fetch + the Polygon 429 burst).
- **#7** — Holdings table: user-selectable **sort**, **company name**, **total-value** column (+ per-currency portfolio total). Migration 0010 added `equity_scores.name`.
- **#9** — Archived/superseded holdings **view** with Restore + Delete. *(One sub-item deferred — see Track C below.)*
- **Received (12m)** column — the home for the real broker dividends that the income/yr fix demoted to fallback.

## ✅ Done in Sprint 2 (2026-06-12, prod `a2289d5`)
- **Track A #1 — Watchlist UI** *(was #2)*. New `tracked_tickers` CRUD route (Free→403, **Pro cap 50** —
  Glenn raised it from the schema's ≤5 — Premium unlimited, 409 on dup). New Pro-gated
  `/app/portfolio/watchlist` page + `watchlist-panel` (reuses TickerSearch + ScoreChip + ScoreDrawer);
  Watchlist tab in the app sub-nav. The 22:30 cron now scores `holdings ∪ tracked_tickers`.
  *Deferred:* the `watchlist_alert` + `weekly_digest` alert emails (enums exist, still unwired) — see Track A below.
- **Track B #3 — Multiple Trading 212 connections per user** *(was #8)*. **Migration 0011 applied to prod**
  (uniqueness relaxed `(user_id, provider)` → `(user_id, provider, wrapper)`). connect/sync/disconnect
  routes now target one connection by id; account UI lists a card per connection + an add-another form
  that hides once both wrappers are connected. sync-brokers cron + reconcile were already
  multi-connection-ready.
- **DROPPED — Reinvest alert (was Track A #2 / old #3).** Glenn cut the `reinvest_opportunity` 3rd alert
  type from scope on 2026-06-12. Scaffolding (`reinvest_opportunity` enum, `send-score-alerts`,
  `score-alert.tsx`) stays in place but unwired; revisit only if it re-enters scope.

## ✅ Done 2026-06-12 (prod `912d2e3`)
- **Track A #1 (part) — Watchlist alert emails.** Pro users can opt into "Watchlist alerts" and get the
  same threshold crossings on tickers they *watch but don't hold*, folded into the existing once-a-day
  score digest (no new cron, no second email, one idempotent send key). Shipped (strict TDD, 693 tests):
  - `lib/alerts/watchlist-selection.ts` — pure `watchedNotHeld()` dedup (watched ∖ held).
  - `send-score-alerts` cron — extracted a shared `buildObservations()` helper; runs a second
    `buildDigest()` over `tracked_tickers ∖ holdings` (BOTH directions at the user's risk/quality
    thresholds, or the defaults, independent of their holdings prefs); folds it into the same email;
    stamps `last_sent_at` across all three event types.
  - `emails/score-alert.tsx` — optional "On your watchlist" section.
  - `/api/notifications` GET/PUT + prefs form + page — a Pro-gated watchlist on/off toggle (no separate
    threshold; it reuses the risk/quality levels).
  - **No migration** — `watchlist_alert` already existed in the `notification_preferences` enum (0004);
    **no new cron** — reuses the 07:00 UTC `send-score-alerts` schedule.
  - **Smoke-verified end-to-end** against a Pro account with an isolated synthetic ticker: `sent:1`,
    idempotent re-run `sent:0`, `sent_emails` dedup row recorded, then torn down. Reusable harness at
    `scripts/smoke/watchlist-alert-dryrun.mjs` (`inspect`/`seed`/`teardown`). This also closes the open
    Sprint-2 watchlist smoke item.
  - *Still deferred under Track A #1:* `weekly_digest` (enum exists, unwired) — scoped OUT of this sprint.

## ✅ Done 2026-06-12 (prod `a191a12`)
- **On-demand score refresh ("Refresh scores" button)** — *(was "Spec'd, not yet built")*. Pro button on
  Portfolio Manager + Watchlist that scores the user's missing/stale(>24h) tickers on demand instead of
  waiting for the nightly cron. Solves the "everything shows Collecting… until the nightly run" gap after
  connecting a portfolio. Shipped (5 commits, strict TDD, 680 tests):
  - Extracted `lib/scoring/score-ticker.ts` (`scoreTicker`/`fetchTickerBundle`/`loadPriorHistory`/
    `topSignalRows`/`isoDateOffset`) from the cron so the on-demand endpoint and the nightly cron share
    ONE code path; cron route refactored to call it, cron tests unchanged.
  - `POST /api/portfolio/refresh-scores` — Pro-gated (Free→403). Diffs `holdings(archived_at null) ∪
    tracked_tickers` vs `equity_scores.computed_at`; scores ≤20 eligible (missing-first), 24h skip,
    15-min cooldown anchored on `profiles.last_score_refresh_at`. Service-role admin for scoring + stamp.
  - `RefreshScoresButton` on both Pro page headings (useTransition + `router.refresh()`; "Score the next
    20" on `remaining>0`; "Try again in ~M min" on 429).
  - **Migration 0012 (`profiles.last_score_refresh_at`) APPLIED to prod + verified.**
  - **Cooldown decision (resolved a spec ambiguity):** 429 fires only when `missing===0 && stale>0 &&
    <15min`. Missing always bypasses; a fully-fresh portfolio returns `{upToDate:true}` (never 429).
    Tradeoff: >20 *stale* tickers wait 15min between batches (over-cap "next 20" re-click only always
    works when the remaining are *missing*). Revisit if stale re-clicks should bypass too.
  - Plan: `docs/superpowers/plans/2026-06-12-on-demand-score-refresh.md`. **Not yet smoke-tested:** manual
    Pro end-to-end click-test + FMP-rate spot-check (need a live Pro session).

## ✅ Done 2026-06-13 (prod `13d1a34`)
- **Track B #2 — CSV import (generic, broker-agnostic).** *(was #1)* A Pro-gated importer that populates
  holdings from any broker export (or a hand-built sheet) the user maps onto a small documented column
  set — the "won't paste an API key" path and the Phase 5 multi-broker bridge. Shipped (strict TDD, 35
  new tests, suite 728 green):
  - `lib/brokers/csv-import.ts` (pure) — `parseCsvHoldings` (papaparse; header-aliased
    ticker/symbol, quantity/shares/qty, avg_cost/cost/price…; BOM-safe; per-row validation with
    line-numbered errors; currency/wrapper defaults) + `buildCsvImportPlan` (upload-safe reconcile).
  - `POST /api/portfolio/import/csv` — Pro-gated (Free→403), native multipart `formData()`, `dryRun`
    preview (with scoring-universe annotation: unknown tickers flagged "value-only") vs apply; reuses
    `runBrokerSync`'s insert→update→archive order via the RLS server client. ~1 MB / 2000-row caps.
  - `import-csv-launcher` + `import-csv-modal` (upload → preview table → confirm), Pro-only button on
    Portfolio Manager; `public/portfolio-import-template.csv` + "Download template" link.
  - **No migration** — `holdings.source` already allows `'csv'`, `connection_id` nullable. CSV holdings
    write `source='csv'`, `connection_id=null`, `external_ref=<scoring ticker>` (no T212 ticker-map / GBX
    conversion — the user supplies our scoring ticker + a 3-letter currency directly). `papaparse` added.
  - **DELIBERATE divergence from T212 sync:** a re-import does NOT archive csv holdings the file omits
    (uploads are error-prone; removal stays with the archived/delete UI). Re-upload is idempotent
    (matches existing csv rows by (ticker, wrapper) → UPDATE, no duplicates); a csv row supersedes a
    manual entry with the same (ticker, wrapper). Live Pro smoke passed 2026-06-13.
  - **Scope:** positions/holdings only. *Deferred fast-follow:* a dividends CSV (same pure-builder
    pattern, a second row-type → `user_dividends`).

---

## Remaining

### Track A — Pro feature completeness
#### 1. Weekly digest email  *(last piece of the alert set; `watchlist_alert` shipped 2026-06-12)*
- The per-cross `watchlist_alert` shipped (see Done above). The remaining enum value is `weekly_digest`:
  a once-a-week summary of holdings ∪ watchlist score movement over the past 7 days. Enum value exists in
  `notification_preferences`, no cron/email path. Unlike the watchlist alert (which reused the daily
  digest), this needs its own weekly schedule + a new template + an iso-week send key. Reuses the
  `send-score-alerts` + `score-alert.tsx` + `sendIdempotent` stack.

### Track B — Broker depth
#### 2. CSV import (broker export upload)  ✅ SHIPPED 2026-06-13 (prod `13d1a34`)
- Positions/holdings import done as a generic, broker-agnostic importer (see Done above).
- *Remaining fast-follow:* a **dividends CSV** (ticker, amount, currency, paid_on → `user_dividends`,
  `source='csv'`) — same pure-builder pattern, scoped out of the positions sprint.

### Track C — Smaller follow-ups
#### 3. "Remove all synced holdings" on disconnect  *(leftover from #9)*
- The archived view (Sprint 1) covers see/restore/delete per row. Still missing: a bulk "remove all synced holdings"
  action at broker-disconnect time, for users who want a clean slate (today the synced rows are kept as a frozen snapshot).
- Low urgency (no data loss). Note: disconnect now targets one connection by id (Sprint 2), so the bulk
  action should scope to the connection being disconnected.

### Track D — Scoring-engine calibration *(touches the frozen engine — needs Glenn's explicit go + its own pass)*

#### 4. GATE_6 payout-ratio sanity gate  *(was #4)*
- A NEW quality gate proposed on `phase-2.75-backtest` (commit `60698a9`). Deferred out of the Stage-0 engine-correctness
  release because it's a policy/behaviour change, not a "wrong number" fix — belongs in a calibration pass where its
  effect on cron output can be judged on its own.

#### 5. Special-dividend exclusion in cut detection  *(was #5)*
- GATE_2 + R1 detect cuts via trailing-12-month YoY decline; a one-off SPECIAL dividend inflates one TTM window so the
  next normal year reads as a spurious >10% "drop" (observed on DLO). Fix: flag any payment >~2× the trailing median
  regular payment that doesn't recur, or use FMP `dividend` vs `adjDividend` divergence as a special marker. Lives in
  `lib/scoring/dividend-cut.ts`.

---

## Spec'd, not yet built
- *(none — on-demand score refresh shipped 2026-06-12; see "Done 2026-06-12" above.)*

## Not yet smoke-tested (Sprint 2)
- Connect a T212 ISA + Invest, sync each, confirm both coexist and disconnect-one leaves the other.
- ~~Add a watchlist ticker the user doesn't hold and trigger the cron~~ — DONE 2026-06-12 via the
  watchlist-alert dry-run (`scripts/smoke/watchlist-alert-dryrun.mjs`).

---

## Suggested next
CSV import shipped + live-smoked (2026-06-13, prod `13d1a34`). Remaining feature work, in order:
1. **Track A #1 (weekly digest)** — small: finishes the alert set. Its own weekly cron + template.
2. **Track C #3 (bulk remove on disconnect)** — low-urgency cleanup, a one-route change.
3. **Dividends CSV** (Track B fast-follow) — a second row-type on the importer just shipped.

Then **Track D** (GATE_6 + special-dividend exclusion) is a separate, deliberate engine-calibration pass
needing Glenn's explicit go — don't fold it into a feature sprint.

After Tracks A + C land, Phase 3.5 is feature-complete and only the Track-D engine pass remains.
