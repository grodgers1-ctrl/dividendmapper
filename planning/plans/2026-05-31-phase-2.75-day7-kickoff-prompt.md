Phase 2.75 Day 7 — Portfolio Manager page (ledger ⇄ manager split) + Buy×Risk quadrant map + admin audit. CONTINUE the sprint.

Working dir: C:\Users\grodg\dividend_mapper_plan. App lives in dividendmapper/.
Stack: Next.js 16.2.4 + React 19.2.4 + Tailwind v4. IMPORTANT: dividendmapper/AGENTS.md
says read node_modules/next/dist/docs/ before writing any Next-specific code — the repo
uses Next 16 / React 19 patterns that may post-date your training data (e.g. the
react-hooks/purity rule forbids Date.now() in server components; react-hooks/
set-state-in-effect forbids synchronous setState in useEffect — both bit earlier days).

══════════════════════════════════════════════════════════════════════════════
CRITICAL CONTEXT — READ TWICE. The scoring system was reframed, most engine changes
were deferred, and one correctness fix WAS shipped. Getting this wrong ships a
disproven claim or re-deploys a frozen engine change.
══════════════════════════════════════════════════════════════════════════════

1) THE REFRAME IS LIVE (display only). "Buy Score" is "Quality Score" everywhere in the
   UI. Internal identifiers + DB columns are UNCHANGED (still buy_* / buyScore — only the
   *display* layer renamed). Action hints flag Risk/Trim only (a high Quality Score is
   descriptive, NOT "buy more"). A public /scoring-methodology page is live, framing the
   score as a "resilience check, not a buy recommendation."

2) ENGINE DEFERRAL — mostly still in force, with ONE exception now live.
   - DEFERRED (still on `phase-2.75-backtest`, NOT deployed, NOT on main): the GATE_1
     financials carve-out, GATE_6 (dividend>EPS) payout gate, and the A4 dividend-growth
     signal. The constraint from Glenn stands: do NOT change what those compute; a future
     deliberate engine release (with cron monitoring) re-adds the trimmed methodology
     claims. Do not merge/deploy them.
   - ⚠️ EXCEPTION — NOW LIVE: the dividend-cut detector (`lib/scoring/dividend-cut.ts`) was
     rewritten and DEPLOYED on Day 6B. The old trailing-TTM-sum logic false-tripped GATE_2/
     R1 on strictly-increasing payers (PEP, a Dividend King, flipped to DNQ on a cron
     recompute). Replaced with per-payment year-over-year matching (immune to window
     clipping + semi-annual asymmetry + one-off specials; still catches real cuts/
     suspensions). PEP is back to a real Quality score; no gate-passer regressed. This is a
     CORRECTNESS fix to existing GATE_2/R1, NOT one of the three deferred items. It does NOT
     change the methodology page (gates' meaning/existence unchanged). Glenn explicitly
     lifted the cron-freeze for this one fix.

3) PHASE 3 (DDM "ideal buy range") IS A CLOSED NO-GO. Redirect alpha is disproven THREE
   ways (Buy-Score redirect, A4, DDM-discount all lose to DRIP). Do NOT build, revive, or
   reference the DDM as an action signal. The defensive Quality + Risk + Trim reframe is the
   FINAL product position. (See `project_buy_score_backtest_artifact` memory.)

4) REINVEST = HYGIENE, NEVER ALPHA. The Reinvest Recommender (shipped Day 6B) is framed as
   diversification / income-smoothing only — never "outperform / better total return."
   Candidates at/over the 20% concentration cap are EXCLUDED. Keep that framing intact.

══════════════════════════════════════════════════════════════════════════════
WHAT'S LIVE IN PROD NOW (dividendmapper.com), all on `main`:
══════════════════════════════════════════════════════════════════════════════
  - `/app/portfolio` (Pro/founding) currently carries the WHOLE analytical stack:
      • holdings table with Quality/Trim/Risk score chips + breakdown drawer (Day 6A),
        gate-failer "DNQ" chips, "Collecting…" pills for not-yet-scored holdings;
      • a collapsible `<ReinvestCard>` ("Dividend due soon" → "See reinvest ideas" ⇄
        "Hide ideas" toggle; Save idea / Not now / Show more; logs to reinvest_suggestions_log);
      • a merged `<PortfolioInsights>` panel (flagged holdings + concentration in ONE box;
        replaced the old PortfolioSummaryBanner + ConcentrationWarning);
      • the income chart; the "View all scores →" link already points at
        `/app/portfolio/scoring` (the Day-7 target page, NOT YET BUILT — currently 404/empty).
  - The nightly cron (22:30 UTC) persists scores + each ticker's next ex-dividend
    (next_ex_div_date/amount/pay_date on equity_scores, migration 0005) using the
    pre-A4/pre-GATE6/pre-carve-out engine PLUS the fixed dividend-cut detector.
  - `/scoring-methodology` (trimmed to the live engine). `/pricing` markets the scoring.
  - UK (.L) income via FMP; concentration via lib/portfolio/concentration.ts.

  ⚠️ CRON HEALTH: on 2026-05-31 the stored scores were 2 days stale (computed_at 2026-05-29)
     before a manual trigger — the scheduled 22:30 cron may have stopped firing. CHECK the
     Vercel cron logs / computed_at freshness early. Also SBRY.L is a newer holding that was
     unscored at last check; it should pick up on the next successful run.

══════════════════════════════════════════════════════════════════════════════
BRANCH + DEPLOY MODEL (unchanged, important):
══════════════════════════════════════════════════════════════════════════════
  - `main` == LIVE PROD EXACTLY (tip `0b6d8eb` at handoff). main was fast-forwarded to
    include all Day-6B + the detector fix + the polish, then CLI-deployed. tsc clean on main.
  - `origin/main` is STALE (~104+ commits behind local main) and was deliberately NOT pushed
    — pushing it would publish 104 commits at once AND trigger the (flaky) productionBranch
    webhook build. Deploys are done via the Vercel CLI from the working tree. Keep local
    `main` ≥ prod.
  - `phase-2.75-backtest` holds the DEFERRED engine changes + all Phase-3 DDM work. NOT
    deployed, NOT merged, on purpose.
  - `phase-2.75-day6b` is now redundant (fully FF'd into main) — safe to delete.
  - ⇒ Base Day-7 work off `main`. Recommended: `git checkout main && git checkout -b
    phase-2.75-day7`; commit there; FF into main at the deploy checkpoint; CLI-deploy from main.
  - HARD RULES:
      * NEVER merge `phase-2.75-backtest` into `main` (carries deferred engine changes).
      * NEVER touch or commit files under dividendmapper/scripts/backtest/ (not part of the
        app build; full workstream lives on the backtest branch).
      * Treat any `main` push as a production deploy. Don't push main casually.
  - Deploy (CLI; webhook flaky): cd dividendmapper; set -a && source .env.local && set +a;
    npx vercel deploy --prod --token "$VERCEL_TOKEN" --yes  → builds the working tree,
    aliases dividendmapper.com. Confirm outward-facing deploys with Glenn; you can't smoke
    the authenticated Pro view yourself (needs his session). Verify on dividendmapper.com.
    NOTE: the auto-mode classifier may block a prod deploy of a compute/outward change until
    Glenn explicitly confirms — surface it and ask, don't work around it.

══════════════════════════════════════════════════════════════════════════════
DAY 7 SCOPE (headline = Glenn's restructure, folding in the roadmap's quadrant + audit):
══════════════════════════════════════════════════════════════════════════════
Glenn reviewed the live portfolio screenshot (planning/plans/dm_portfolioview31_05_26.png)
and wants the analytical layer split OFF the holdings ledger into a dedicated page. Day-6B
shipped a quick declutter (collapsed reinvest card, merged insights panel); Day 7 is the
proper structural split, PLUS the roadmap's quadrant map + admin audit.

  1. STRIP `/app/portfolio` back to a clean LEDGER: ticker, wrapper, quantity, cost,
     income, broker. Remove the score chips, the PortfolioInsights panel, and the
     ReinvestCard from it. (Free tier already only sees the ledger + upgrade pills.)
  2. NEW "Portfolio Manager" page (Pro/founding) that HOSTS the analytical layer:
     • per-holding Quality/Trim/Risk chips + the breakdown drawer (move/reuse from Day 6A),
     • the `<PortfolioInsights>` panel,
     • the `<ReinvestCard>`,
     • the NEW Buy×Risk QUADRANT MAP (the genuinely new build — see DESIGN GAP C).
  3. NAVIGATION between the ledger and the Manager page (see DESIGN GAP E).
  4. Admin audit dashboard at `/admin/scoring/audit` (admin-only via isAdmin in
     lib/scoring/config) — scope TBD (see DESIGN GAP D); may be trimmed/deferred.

The roadmap synopsis (planning/07-phase2.75-and-3-synopsis.md) anchors this:
  - line 24: "score chips + breakdown drawer on the holdings table; Buy×Risk quadrant map at
    /app/portfolio/scoring; reinvest suggestion card on /app/portfolio"
  - line 28: "internal audit dashboard at /admin/scoring/audit"
  - line 32: Pro tier = full scoring + reinvest + quadrant + wizard + alerts + watchlist.
  (The full equity-scoring spec `planning/06-equity-scoring.md` is NOT on main — it lives on
  phase-2.75-backtest. Don't checkout that branch to read it; the synopsis + the existing
  Day-6A components are sufficient. If you must see the quadrant spec, `git show
  phase-2.75-backtest:planning/06-equity-scoring.md` is read-only and safe.)

══════════════════════════════════════════════════════════════════════════════
DESIGN GAPS — RESOLVE WITH GLENN BEFORE BUILDING (brainstorming + plan mode, pause for sign-off):
══════════════════════════════════════════════════════════════════════════════
  A. ROUTE + NAMING. The existing "View all scores →" link points to `/app/portfolio/scoring`,
     and the synopsis uses that path. Glenn calls the page "Portfolio Manager." Decide: keep
     the route `/app/portfolio/scoring` (no link changes) but TITLE it "Portfolio Manager", OR
     use `/app/portfolio/manager` (and update the link). Recommend the former (less churn).
  B. LEDGER vs MANAGER CONTENT SPLIT. Confirm exactly what leaves the ledger. Does the ledger
     keep ANY score hint (e.g. a tiny action dot) or go fully clean? Is the holdings table
     DUPLICATED (a plain ledger table vs a manager table with chips) or ONE shared
     `<HoldingsTable>` with a `showScores` prop? (Recommend a shared table with a prop — the
     reframe memory `feedback_duplicate_over_share_roadmap_boundary` says default to
     duplication across roadmap boundaries, but this is the SAME surface split, so a prop is
     cleaner — confirm.)
  C. QUADRANT MAP DESIGN (the substantive new build). Buy(Quality)×Risk scatter. Resolve:
     axes orientation + labels (e.g. x=Risk →, y=Quality ↑; four labelled quadrants like
     "High quality / low risk = core" …); what a point is (one per scored gate-passing
     holding); bubble size (position weight? equal?); colour (by Trim? by quadrant?); how
     GATE-FAILERS + unscored holdings are shown (excluded? listed separately?); click → open
     the score drawer; and the MOBILE fallback (a scatter is unreadable on a phone — a sorted
     list/table fallback below a breakpoint?). Decide the charting approach (hand-rolled SVG
     vs a lib already in the repo — check package.json before adding any dependency). Run the
     humaniser linter on all quadrant copy.
  D. ADMIN AUDIT SCOPE. What does `/admin/scoring/audit` show, and is it Day 7 or deferred?
     Candidates: cron-run health (last computed_at, success/fail counts), score distribution,
     data-quality flags (sparse/degraded_uk counts), gate-failure breakdown, tickers with
     stale/missing scores. Keep it admin-only (isAdmin). Recommend a small read-only v1 or
     defer if Day 7 is already large.
  E. NAVIGATION. How do users move ledger ⇄ manager? Tabs at the top of /app/portfolio, or a
     prominent link/button, or a nav entry in app/app/layout.tsx? Mobile pattern too.
  Then present a recalibrated Day-7 plan for approval. NO code until A–E are decided and the
  plan is signed off. (Per feedback_lead_with_recommendation: lead with clear recs, don't pad.)

══════════════════════════════════════════════════════════════════════════════
START BY (in order):
══════════════════════════════════════════════════════════════════════════════
  1. Invoke superpowers:executing-plans (announce it). Use brainstorming + writing-plans for
     the Day-7 recalibration (it is NOT yet detailed — detailing it is your job).
  2. Read these auto-memories FIRST (most important first):
       - state_2026-05-31_day6b_detector_fix_and_reinvest  (THE current state: detector fix
         live + Reinvest card + the polish + the Portfolio-Manager Day-7 intent + deploy model)
       - project_buy_score_backtest_artifact  (reframe + engine deferral + Phase 3 NO-GO +
         branch/deploy model)
       - state_2026-05-30_eod_day6a_phase2.75  (the scoring UI components you'll be moving:
         ScoreChip, ScoreDrawer, gate-reasons, chip-display, holdings-table wiring)
       - reference_equity_scoring_spec, reference_roadmap_synopsis,
         reference_fmp_coverage_matrix, feedback_phase_namespace_gotcha (the two unrelated
         "Phase" namespaces — relevant when auditing), feedback_humaniser_mandatory +
         reference_humaniser_linter (copy), project_vercel_webhook_flaky (deploy).
  3. CONFIRM BASELINE BEFORE WRITING CODE:
       a. cd dividendmapper && npm test  → expect ~397 unit tests / 65 files green (re-run once
          if 1–2 transient cold-worker failures).
       b. cd dividendmapper && npx tsc --noEmit → clean.
       c. git -C C:/Users/grodg/dividend_mapper_plan log --oneline -1 main → tip `0b6d8eb` ==
          live prod. Base Day 7 off main. Confirm you are NOT on phase-2.75-backtest.
       d. Query live equity_scores (ticker, buy/trim/risk, gate, data_quality, computed_at) and
          check cron freshness; confirm SBRY.L scored; note any new holdings/tickers.
  4. Resolve design gaps A–E with Glenn, recalibrate the Day-7 plan, get sign-off, THEN execute.

══════════════════════════════════════════════════════════════════════════════
EXECUTION DISCIPLINE (unchanged from Days 2–6B):
══════════════════════════════════════════════════════════════════════════════
  - Strict TDD: failing test → run → impl → run → commit, one logical unit per commit.
  - Component tests: RTL + jsdom (*.test.tsx). vitest globals:false — import
    {describe,it,expect,vi,beforeEach,afterEach} explicitly from "vitest". RTL cleanup is in
    vitest.setup.ts (afterEach(cleanup)) — don't remove. jsdom env is set by a *.test.tsx glob
    in vitest.config.ts; running vitest from a DRIFTED cwd → "document is not defined" (cwd
    bug, not your code — always `cd /c/Users/grodg/dividend_mapper_plan/dividendmapper &&` your
    npm/test/tsc/vitest commands; run git from the repo root).
  - API/route + drawer patterns to reuse: ScoreDrawer uses @base-ui/react/dialog + lucide-
    react; auth routes use supabase.auth.getClaims() → claims.sub; runtime="nodejs",
    dynamic="force-dynamic"; mock @/lib/supabase/server in route tests. Admin pages gate on
    isAdmin (lib/scoring/config).
  - Match existing /app/portfolio surfaces. No em dashes in user-facing copy (colon/comma
    convention); the no-data "—" glyph is fine. Run scripts/lint/humaniser.js --strict on ALL
    new user-facing copy and show the 24-pattern audit before presenting to Glenn.
  - Edit existing files via Edit (Read first). Write only for new files.
  - Commits: HEREDOC, end with:
      Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
  - Pause at "END OF DAY 7 CHECKPOINT" for Glenn review. Do not start Day 8.

══════════════════════════════════════════════════════════════════════════════
OPERATIONAL WARTS (known — do NOT re-investigate):
══════════════════════════════════════════════════════════════════════════════
  - Bash cwd DRIFTS after `cd` inside a command. Prefix every npm/test/tsc/vitest with
    `cd /c/Users/grodg/dividend_mapper_plan/dividendmapper &&`; run git from repo root.
  - `npx tsc`/`npx vitest` from a drifted cwd error oddly ("not the tsc you're looking for" /
    "document is not defined"). Re-run with the explicit cd.
  - tsc is CLEAN on main. `npm run lint` has 3 PRE-EXISTING errors in files unrelated to this
    work (ticker-search.tsx, locale-toggle.tsx, levers-card.tsx — react-hooks/set-state-in-
    effect) + a couple of backtest-scaffold warnings; they do NOT fail the build. Don't fix
    them (out of scope) but don't add new ones.
  - Vitest may report 1–2 transient failures on a cold first run (worker timing); re-run once.
  - Supabase ad-hoc SELECT: cd dividendmapper; set -a && source .env.local && set +a &&
    npx supabase db query --linked "<sql>". Use it to ground the plan (live equity_scores,
    holdings per user, cron freshness). Two users exist; "Glenn" = user_id
    3fbb56f5-9a18-4ef5-a58b-575fd96be25e (PEP×2, MSFT 100, VOD.L 1800, PYPL 34, DLO, LGEN.L,
    SBRY.L, SCHD — MSFT dominates ~89% by value).
  - LSE caveat: stored current_yield for .L is unreliable — use dividend_per_share.
  - .env.local line ~44 emits "pbfe: command not found" when sourced — harmless, ignore.
  - vercel.json maxDuration capped at 300s (Hobby). Don't bump.

══════════════════════════════════════════════════════════════════════════════
NOT IN SCOPE FOR DAY 7 (parked):
══════════════════════════════════════════════════════════════════════════════
  - DEFERRED ENGINE CHANGES (GATE_1 financials carve-out, GATE_6, A4) — on phase-2.75-backtest,
    NOT to be merged/deployed. (The dividend-cut detector fix is ALREADY live and is separate.)
  - Phase 3 DDM — CLOSED NO-GO.
  - Personalisation wizard + /api/preferences + public per-ticker /scoring page + sitemap — Day 8.
  - Notification prefs + alert emails (Buy/Risk/Reinvest) + 23:00 cron — Day 9.
  - Watchlist (≤5) — polish backlog.
  - Giving "Save idea" a real destination (Add-Holding prefilled / drawer) — deferred by Glenn.

Then: resolve A–E, recalibrate the Day-7 plan, get Glenn's sign-off, and only then execute.
No code until baseline confirmed + design gaps decided + plan approved.

---
Provenance: written 2026-05-31 at the end of the Day-6B session (Reinvest Recommender + ex-div
data layer + dividend-cut detector fix + UI declutter all shipped to prod; main == prod @
0b6d8eb). Source-of-truth memory: state_2026-05-31_day6b_detector_fix_and_reinvest.
